01-domain

Estructura inicial del dominio siguiendo DDD. Contiene únicamente interfaces y value objects.

Reglas:
- No depender de librerías externas.
- Solo tipos, interfaces y contratos de servicio en esta carpeta.
- Implementaciones pertenecen a la capa de `infrastructure`.
